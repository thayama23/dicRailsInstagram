<h1><%= @user.name %>のページ</h1>

<p>メールアドレス: <%= @user.email %></p>
<p>フォト: <%= @user.photo.to_s %></p>
<br>
<%= link_to "ユーザー編集設定画面へ", edit_user_path %>
<br>
<%= link_to "ブログ一覧画面にもどる", blogs_path %>

<br>
<%= link_to '新しくBlogを投稿する', new_blog_path %>

<br>
<h2><%= @user.name %>さんのお気に入りブログリスト</h2>

<table>
  <tr>
    <th>ユーザーフォト</th>
    <th>ユーザー</th>
    <th>タイトル</th>
    <th>内容</th>
    <th>写真投稿</th>
  </tr>
<% @blogs.each do |blog| %>
  <tr>
    <td><%= image_tag blog.user.photo.to_s%></td>
    <td><%= blog.user.name%></td>
    <td><%= blog.title %></td>
    <td><%= blog.content %></td>
    <td><%= image_tag blog.image.to_s %></td>
  </tr>
<% end %>
</table>
